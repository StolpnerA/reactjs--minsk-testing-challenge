const PRIORITY_OPTIONS = [
  '',
  'Low',
  'Medium',
  'High',
];

const COLUMNS = ["id", "done", "title", "priority", "date"]

const STORAGE_KEY = 'tasks'

export {
  PRIORITY_OPTIONS,
  COLUMNS,
  STORAGE_KEY,
};